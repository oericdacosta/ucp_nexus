# Server Configuration
UCP_HOST=0.0.0.0
UCP_PORT=10101
UCP_LOG_LEVEL=INFO


# Default UCP Server (Optional)
# If set, this avoids needing to pass the URL manually every time
# UCP_UCP_SERVER_URL=http://your-ucp-server-url:port

# Advanced Configuration (Optional)
# UCP_HTTP_TIMEOUT=10.0
# UCP_JWT_EXPIRY_SECONDS=300
